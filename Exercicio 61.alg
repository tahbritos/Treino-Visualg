algoritmo "RepeticaocomFacaEnquanto"


var
      P, U, I, n: Inteiro
inicio
       P <- 0
       U <- 30
       I <- 3

       Escreva (P)
       Enquanto (n < U) faca
            n <- n + P
            n <- N + I
            Escreva (N)
       FimEnquanto
fimalgoritmo
